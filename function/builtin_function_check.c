#include<stdio.h>
#include<math.h>
int main()
{
    double a =10.01;
    double resuelt=ceil(a);
    printf("the ceil number-%lf\n",resuelt);
    double b=10.01;
    printf("the floor number-%lf\n",floor(b));
    double c=10.01;
    printf("the round number-%lf\n",round(c));
    double d=9;
    printf("the squart number%lf\n",sqrt(d));

    printf("the pow number -%lf",pow(5,3));


    
}